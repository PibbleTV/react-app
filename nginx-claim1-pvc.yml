apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-claim1
  namespace: default
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi